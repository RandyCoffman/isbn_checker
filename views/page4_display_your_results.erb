<!DOCTYPE html>
<html>
<head>
	<title>final</title>
</head>
<body>
	<h3>Here are all of your searched ISBN numbers.</h3>

	<% CSV.open('new_results.csv', 'w') do |csv| %>
  		<% CSV.read('results.csv').uniq.each { |r| csv << r } %>
	<% end %>

	<% CSV.open("new_results.csv", 'rb') do |row| %>
		<div>
			<% if row[0].include?("#{first_name} #{last_name}") == true %>
					<%= row[0] %>
  			<% end %>
  		</div>
	<% end %>
	<% connect2 %>
</body>
</html>